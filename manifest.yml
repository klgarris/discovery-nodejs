declared-services:
  my-discovery:
    label: discovery
    plan: standard
applications:
  - name: klg-discovery-news-demo
    command: npm start
    path: .
    memory: 512M
    services:
      - my-discovery
